<script src="<%= includejs %>"></script>
<script src="/jwcrypto.js"></script> <!-- TODO: this should be loaded by BID -->
<script src="/loggedin.js"></script>

<p id="message"></p>

<script>
var identity = "<%= identity %>";
<% if (wrappedKey) { %>
var wrappedKey = "<%= wrappedKey %>";
doUnwrap(identity, wrappedKey);
<% } else { %>
doGenerate(identity);
<% } %>
</script>
